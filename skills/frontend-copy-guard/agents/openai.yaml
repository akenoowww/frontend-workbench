interface:
  display_name: "Frontend Copy Guard"
  short_description: "Keep affected interface copy product-safe"
  default_prompt: "Use $frontend-copy-guard to fix the affected interface copy without exposing internal details."
