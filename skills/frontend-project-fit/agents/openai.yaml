interface:
  display_name: "Frontend Project Fit"
  short_description: "Reuse the host frontend before inventing"
  default_prompt: "Use $frontend-project-fit to implement this frontend change with the project architecture and existing reusable UI."
