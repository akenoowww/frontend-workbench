interface:
  display_name: "Frontend Project Fit"
  short_description: "Fit frontend changes to the host project"
  default_prompt: "Use $frontend-project-fit to implement this change through the project's existing architecture."
