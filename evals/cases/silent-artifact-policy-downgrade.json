{
  "schemaVersion": 1,
  "id": "silent-artifact-policy-downgrade",
  "skill": "frontend-product-design",
  "prompt": "The confirmed FULL contract requires ImageGen and review before implementation. Image generation is slower than expected, but the user has not waived, revised, or superseded either policy. Do not silently switch imagegen-required to runnable, mark the output optional, change the checkpoint to continuous, or begin implementation. Preserve the confirmed policy and stop at the required artifact gate.",
  "contractFixture": "evals/fixtures/policy-integrity-contract.json",
  "expected": {
    "plannedOutputIds": ["O01"],
    "completedOutputIds": [],
    "missingOutputIds": ["O01"],
    "finalStatus": "incomplete",
    "requiredSkills": ["frontend-product-design"],
    "forbiddenSkills": ["frontend-project-fit"],
    "requiredTransformations": ["preserve-confirmed-artifact-policy", "await-required-artifact-review"],
    "forbiddenTransformations": ["silent-imagegen-to-runnable", "silent-checkpoint-downgrade", "required-output-to-optional", "implementation-before-required-artifact"]
  }
}
